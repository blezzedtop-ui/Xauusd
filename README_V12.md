# XAUUSD V12 - Gold Liquid Glass + Mobile + Modules

This package is rebuilt from the uploaded V11 source. Existing Gold Liquid Glass design is preserved. Backend/frontend syntax was validated before packaging.

Railway variables:
- REALMARKET_API_KEY
- MARKET_PROVIDER=auto
- ALLOW_DEMO=false
- DATABASE_URL (Postgres reference)

Deploy by replacing repository files with this package contents.
