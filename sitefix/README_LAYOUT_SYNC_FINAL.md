SignalX layout sync final

XAUUSD index.html and EURUSD eurusd.html now share the same outer UI/layout CSS stack.
The XAU-only legacy neon style blocks were removed because they caused the large blank area and right-shifted hero.
TradingView chart implementation is not edited by this patch.
